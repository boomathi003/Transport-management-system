rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TODO: Replace with authenticated rules before production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
